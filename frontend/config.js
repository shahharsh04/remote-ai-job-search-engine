/*
 * API endpoint configuration.
 *
 * On Netlify this file is REGENERATED at build time from the
 * VITE_API_BASE_URL environment variable (see netlify.toml), so the
 * value below is only the local-development default and never reaches
 * production. The build fails if the variable is missing or points at
 * localhost, so a broken URL cannot ship.
 *
 * Set it in Netlify:
 *   Site configuration -> Environment variables -> VITE_API_BASE_URL
 *   e.g. https://remote-job-search-api.onrender.com   (no trailing slash)
 */
window.APP_CONFIG = {
  API_BASE_URL: "http://127.0.0.1:8000",
};
