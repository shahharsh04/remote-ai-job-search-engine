/*
 * API endpoint configuration.
 *
 * On Netlify this file is REGENERATED at build time from the
 * API_BASE_URL environment variable (see netlify.toml), so the value
 * below is only the local-development default and is never deployed.
 *
 * Set it in Netlify: Site settings -> Environment variables -> API_BASE_URL
 */
window.APP_CONFIG = {
  API_BASE_URL: "http://127.0.0.1:8000",
};
